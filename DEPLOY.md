# Инструкция по публикации на GitHub Pages

## Быстрый старт

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Назовите репозиторий (например, `petro_ml_book`)
4. Сделайте репозиторий **публичным** (для бесплатного GitHub Pages)
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)

### 2. Загрузите код

Откройте терминал в папке проекта и выполните:

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: книга о машинном обучении в геофизике"

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/petro_ml_book.git

# Загрузите код
git push -u origin main
```

Если у вас ветка называется `master` вместо `main`, используйте:
```bash
git push -u origin master
```

### 3. Включите GitHub Pages

**Вариант A: Через настройки репозитория (рекомендуется)**

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите на вкладку **Settings** (Настройки)
3. В левом меню найдите раздел **Pages**
4. В разделе **Source**:
   - Выберите ветку: `main` (или `master`)
   - Выберите папку: `/ (root)`
5. Нажмите **Save**

**Вариант B: Автоматическая сборка через GitHub Actions**

Если вы используете GitHub Actions (файл `.github/workflows/pages.yml` уже создан):

1. Перейдите в Settings → Pages
2. В разделе **Source** выберите **GitHub Actions**
3. GitHub автоматически начнет сборку при следующем push

### 4. Дождитесь публикации

GitHub начнет сборку сайта. Обычно это занимает 1-3 минуты.

Вы можете отслеживать процесс:
- В репозитории: вкладка **Actions** (для GitHub Actions)
- В Settings → Pages: будет показан статус сборки

### 5. Доступ к сайту

После успешной публикации ваш сайт будет доступен по адресу:

```
https://YOUR_USERNAME.github.io/petro_ml_book/
```

Замените `YOUR_USERNAME` на ваш GitHub username.

## Локальное тестирование (опционально)

Если вы хотите проверить сайт локально перед публикацией:

### Установка Ruby и Jekyll

**Windows:**
1. Скачайте RubyInstaller: https://rubyinstaller.org/
2. Установите Ruby (отметьте опцию "Add Ruby executables to your PATH")
3. Откройте новый терминал и выполните:
   ```bash
   gem install bundler
   ```

**macOS:**
```bash
brew install ruby
gem install bundler
```

**Linux:**
```bash
sudo apt-get install ruby-full build-essential zlib1g-dev
gem install bundler
```

### Запуск локального сервера

```bash
# Установите зависимости
bundle install

# Запустите локальный сервер
bundle exec jekyll serve

# Откройте браузер на http://localhost:4000
```

## Обновление сайта

После внесения изменений в файлы:

```bash
# Добавьте измененные файлы
git add .

# Сделайте коммит
git commit -m "Описание изменений"

# Загрузите изменения
git push
```

GitHub автоматически пересоберет сайт через 1-3 минуты.

## Решение проблем

### Сайт не публикуется

1. Проверьте, что репозиторий **публичный**
2. Убедитесь, что в Settings → Pages выбрана правильная ветка
3. Проверьте вкладку **Actions** на наличие ошибок сборки

### Ошибки сборки

1. Убедитесь, что все markdown файлы имеют front matter (запустите `python add_front_matter.py`)
2. Проверьте синтаксис YAML в `_config.yml`
3. Посмотрите логи сборки в разделе Actions

### Стили не применяются

1. Убедитесь, что файл `assets/css/style.css` существует
2. Проверьте пути к CSS в `_layouts/default.html`
3. Очистите кеш браузера (Ctrl+F5)

## Дополнительные настройки

### Изменение темы

Отредактируйте `_config.yml`:
```yaml
theme: minima  # или другая тема
```

### Добавление домена

1. В Settings → Pages добавьте ваш домен
2. Создайте файл `CNAME` в корне репозитория с именем домена
3. Настройте DNS записи у вашего регистратора домена

## Полезные ссылки

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация Jekyll](https://jekyllrb.com/docs/)
- [Jekyll Themes](https://jekyllthemes.io/)

---

**Успешной публикации!** 🚀

